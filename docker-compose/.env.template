# Tiledesk Environment Variables for Render

# External URLs - Replace with your Render service URL (without trailing slash)
# For example, if your Render URL is https://tiledesk.onrender.com
EXTERNAL_BASE_URL=https://your-render-service-url
EXTERNAL_MQTT_BASE_URL=wss://your-render-service-url

# Render Port Configuration - Render sets this automatically, but you can override if needed
# PORT=10000

# MongoDB Connection - Replace with your MongoDB connection string
# You can use MongoDB Atlas (https://www.mongodb.com/cloud/atlas) or another MongoDB provider
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/tiledesk

# OpenAI API Key for GPT actions
OPENAI_API_KEY=your-openai-api-key

# RabbitMQ Configuration
RABBITMQ_ERLANG_COOKIE=tiledesk-cookie
RABBITMQ_DEFAULT_USER=tiledesk
RABBITMQ_DEFAULT_PASS=tiledesk-password

# Optional Firebase Configuration (if you want to use Firebase features)
FIREBASE_APIKEY=
FIREBASE_AUTHDOMAIN=
FIREBASE_DATABASEURL=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGEBUCKET=
FIREBASE_MESSAGINGSENDERID=
FIREBASE_APP_ID=
FIREBASE_VAPID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

# Optional Email Configuration
EMAIL_ENABLED=false
EMAIL_HOST=
EMAIL_USERNAME=
EMAIL_SECURE=
EMAIL_PORT=
EMAIL_PASSWORD=
EMAIL_FROM_ADDRESS=

# Optional License Key
LICENSE_KEY=